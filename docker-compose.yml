version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: findcar-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: findcardb
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: t9e8o4f7
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: